<%if !@question.nil? %>
	<%= render partial: 'shared/practices', collection: @question %>
<% else %>
	<p> There are no more questions!</p>
	<p> You attempted <%= @game.number %> question<% if !(@game.number == 0) %>s<% end %> 
	<% if @game.number != 0 %>
		and got <%= @game.correct %> correct! </p>
		<p> You got <%= (@game.correct*100/@game.number*100) / 100 %>%! </p>
	<%else%>
		</p>
	<% end %>
<% end %>